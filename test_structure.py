from conan.api.conan_api import ConanAPI
from conans.model.recipe_ref import RecipeReference
from conans.model.pkg_ref import PkgReference

conan_api = ConanAPI()
# Create a dummy node info
# It's hard without graph. Let's just create a mock or inspect source.
